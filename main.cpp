#include "matrix.h"
#include <iostream>

using namespace std;

int main(int argc, char* argv[]) {
  double a[6] = {1, 0, 2, 2, 3, 4};
 // Matrix<double> mA(3, 2);
 // cout << mA.get(0, 1) << endl;
 // mA.fill(a);

  return 0;
}
